# InferenceHub

Project scaffold for an inference service with FastAPI and Celery worker support.

## Structure

- `app/` - application source code
- `docker/` - Docker build context for web and worker services
- `docker-compose.yml` - local deployment configuration
- `.env` - environment variables
